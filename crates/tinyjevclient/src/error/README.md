# Error module

The crate-wide `Error` enum classifies configuration, request, transport, HTTP,
decoding, and response-contract failures without retaining credentials or response
bodies. `test.rs` pins safe rendering.
